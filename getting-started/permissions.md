---
description: ezEdits 插件完整权限列表
---

# 权限设置 (Permissions)

权限按功能包进行分组，可以通过授予 `ezedits.permpack.***` 来批量开启。

<table><thead><tr><th width="274">权限</th><th width="327">描述</th><th>权限包</th></tr></thead><tbody><tr><td>ezedits.general.wiki</td><td><strong>/ezedits wiki</strong><br>在聊天中打印此 wiki 的链接</td><td>基础(Basic)</td></tr><tr><td>ezedits.general.info</td><td><strong>/ezedits info</strong><br>在聊天中打印基本插件信息</td><td>基础(Basic)</td></tr><tr><td>ezedits.general.quiet</td><td><strong>/ezedits quiet</strong><br>切换 ezEdits 的部分聊天消息的开/关</td><td>基础(Basic)</td></tr><tr><td>ezedits.admin.reload</td><td><strong>/ezedits reload</strong><br>重新加载配置</td><td>管理员(Admin)</td></tr><tr><td>ezedits.array</td><td><strong>//ezarray</strong><br>沿路径依次放置多个结构</td><td>高级(Advanced)</td></tr><tr><td>ezedits.brush.array</td><td><strong>//ezbr array</strong><br>允许沿绘制的路径放置多个形状或 3D 结构</td><td>高级(Advanced)</td></tr><tr><td>ezedits.brush.gradient</td><td><strong>//ezbr gradient</strong><br>将渐变笔刷绑定到物品</td><td>中级(Intermediate)</td></tr><tr><td>ezedits.brush.gradientstroke</td><td><strong>//ezbr gradientstroke</strong><br>将渐变描边笔刷绑定到物品</td><td>中级(Intermediate)</td></tr><tr><td>ezedits.brush.paletteshift</td><td><strong>//ezbr paletteshift</strong><br>将调色板偏移笔刷绑定到物品</td><td>中级(Intermediate)</td></tr><tr><td>ezedits.brush.scatter</td><td><strong>//ezbr scatter</strong><br>允许在区域内放置多个形状或 3D 结构</td><td>高级(Advanced)</td></tr><tr><td>ezedits.debug</td><td><strong>//ezdebug ...</strong><br>各种调试命令。<br>正常使用不需要</td><td>管理员(Admin)</td></tr><tr><td>ezedits.debug.loadingbar</td><td><strong>//ezdebug clearLoadingBars</strong><br>清除屏幕上的任何活动进度条。</td><td>基础(Basic)</td></tr><tr><td>ezedits.deform.hexagonalize</td><td><strong>//ezdeform hexagonalize</strong><br>将选区中的方块变形为六边形柱体</td><td>中级(Intermediate)</td></tr><tr><td>ezedits.deform.noise</td>

<td><strong>//ezdeform noise</strong><br>基于给定的噪声对选区内的方块进行变形</td><td>中级(Intermediate)</td></tr><tr><td>ezedits.deform.rotate</td><td><strong>//ezdeform rotate</strong><br>旋转选区内的方块</td><td>基础(Basic)</td></tr><tr><td>ezedits.deform.voronoialize</td><td><strong>//ezdeform voronoialize</strong><br>将选区内的方块变形为泰森多边形单元</td><td>中级(Intermediate)</td></tr><tr><td>ezedits.deform.voronoialize2</td><td><strong>//ezdeform voronoialize2</strong><br>将选区内的方块变形为近似输入形状的泰森多边形单元</td><td>中级(Intermediate)</td></tr><tr><td>ezedits.deform.voxelize</td><td><strong>//ezdeform voxelize</strong><br>将选区内的方块变形为体素</td><td>中级(Intermediate)</td></tr><tr><td>ezedits.flow.flowfield</td><td><strong>//ezflowfield</strong><br>在选区内生成流场</td><td>中级(Intermediate)</td></tr><tr><td>ezedits.flow.flowline</td><td><strong>//ezflowline</strong><br>从玩家位置创建单条流线</td><td>中级(Intermediate)</td></tr><tr><td>ezedits.gen.cone</td><td><strong>//ezshapes cone</strong><br>在玩家位置生成圆锥体</td><td>基础(Basic)</td></tr><tr><td>ezedits.gen.polydome</td><td><strong>//ezshapes polydome</strong><br>在玩家位置生成多边形穹顶</td><td>基础(Basic)</td></tr><tr><td>ezedits.gen.polygon</td><td><strong>//ezshapes polygon</strong><br>在玩家位置生成多边形</td><td>基础(Basic)</td></tr><tr><td>ezedits.gen.square</td><td><strong>//ezshapes square</strong><br>在玩家位置生成正方形</td><td>基础(Basic)</td></tr><tr><td>ezedits.gen.tetrahedron</td><td><strong>//ezshapes tetrahedron</strong><br>在玩家位置生成四面体</td><td>基础(Basic)</td></tr><tr><td>ezedits.gen.torus</td><td><strong>//ezshapes Torus</strong><br>在玩家位置生成圆环体</td><td>基础(Basic)</td></tr><tr><td>ezedits.mask.aim</td><td><strong>#aim</strong><br>蒙版匹配目标方块</td><td>基础(Basic)</td></tr><tr><td>ezedits.mask.attached</td><td><strong>#attached</strong><br>蒙版匹配接触相邻方块的方块</td><td>基础(Basic)</td>

</tr><tr><td>ezedits.mask.current</td><td><strong>#current</strong><br>代表你当前的全局蒙版</td><td>基础(Basic)</td></tr><tr><td>ezedits.mask.fullblock</td><td><strong>#fullblock</strong><br>蒙版到占据完整方块空间的方块</td><td>基础(Basic)</td></tr><tr><td>ezedits.mask.infested</td><td><strong>#infested</strong><br>蒙版到包含蠹虫的方块</td><td>基础(Basic)</td></tr><tr><td>ezedits.mask.light</td><td><strong>#truelight | #blocklight</strong><br>蒙版到匹配光照等级范围的方块。<br>Truelight 包含天空光<br>Blocklight 仅考虑来自方块的光照</td><td>基础(Basic)</td></tr><tr><td>ezedits.mask.near</td><td><strong>#near</strong><br>蒙版到内部蒙版给定3D范围内的方块</td><td>中级(Intermediate)</td></tr><tr><td>ezedits.mask.noise</td><td><strong>#eznoisemask</strong><br>蒙版到给定噪声高于给定阈值的位置上的方块</td><td>中级(Intermediate)</td></tr><tr><td>ezedits.mask.palette</td><td><strong>#palette | #fuzzypalette</strong><br>蒙版到给定调色板内的方块。<br>Fuzzypalette 忽略方块状态</td><td>中级(Intermediate)</td></tr><tr><td>ezedits.mask.selection</td><td><strong>#savedselection</strong><br>蒙版到已保存选区区域内的方块</td><td>中级(Intermediate)</td></tr><tr><td>ezedits.mask.vectorgradient</td><td><strong>#vectorgradientmask</strong><br>蒙版到沿给定向量且高于梯度阈值的位置上的方块</td><td>中级(Intermediate)</td></tr><tr><td>ezedits.noise.delete</td><td><strong>//eznoise delete</strong><br>删除已保存的噪声预设</td><td>中级(Intermediate)</td></tr><tr><td>ezedits.noise.list</td><td><strong>//eznoise list</strong><br>列出所有已保存的噪声预设</td><td>中级(Intermediate)</td></tr><tr><td>ezedits.noise.print</td><td><strong>//eznoise print</strong><br>打印已保存噪声预设的参数</td><td>中级(Intermediate)</td></tr><tr><td>ezedits.noise.save</td><td><strong>//eznoise save</strong><br>保存噪声预设</td><td>中级(Intermediate)</td></tr><tr><td>ezedits.palettes.decode</td><td><strong>//ezpalette decode</strong><br>打印给定编码调色板字符串的方块。</td><td>中级(Intermediate)</td></tr><tr><td>ezedits.palettes.delete</td><td><strong>//ezpalette delete</strong><br>删除已保存的调色板</td><td>中级(Intermediate)</td>

</tr><tr><td>ezedits.palettes.encode</td><td><strong>//ezpalette encode</strong><br>将调色板编码为更短的文本字符串</td><td>中级(Intermediate)</td></tr><tr><td>ezedits.palettes.fetch</td><td><strong>//ezpalette fetch</strong><br>从世界或玩家的快捷栏中获取方块并保存为调色板</td><td>中级(Intermediate)</td></tr><tr><td>ezedits.palettes.list</td><td><strong>//ezpalette list</strong><br>列出所有已保存的调色板</td><td>中级(Intermediate)</td></tr><tr><td>ezedits.palettes.place</td><td><strong>//ezpalette place</strong><br>将已保存的调色板放置到世界中</td><td>中级(Intermediate)</td></tr><tr><td>ezedits.palettes.print</td><td><strong>//ezpalette print</strong><br>打印已保存调色板的方块</td><td>中级(Intermediate)</td></tr><tr><td>ezedits.palettes.save</td><td><strong>//ezpalette save</strong><br>保存调色板</td><td>中级(Intermediate)</td></tr><tr><td>ezedits.palettes.swap</td><td><strong>//ezpalette swap</strong><br>将所有匹配的方块替换为另一个调色板中的对应方块</td><td>中级(Intermediate)</td></tr><tr><td>ezedits.pattern.aim</td><td><strong>#aim</strong><br>使用目标方块作为图案</td><td>基础(Basic)</td></tr><tr><td>ezedits.pattern.noise</td><td><strong>#eznoisepattern</strong><br>使用噪声预设来放置调色板方块</td><td>中级(Intermediate)</td></tr><tr><td>ezedits.pattern.palette</td><td><strong>#palette</strong><br>将调色板中的方块视为简单列出的方块</td><td>中级(Intermediate)</td></tr><tr><td>ezedits.pattern.selection</td><td><strong>#selection</strong><br>根据已保存选区位置在世界中当前存在的内容设置方块。就像该区域被平铺/堆叠一样</td><td>中级(Intermediate)</td></tr><tr><td>ezedits.pattern.vectorgradient</td><td><strong>#vectorgradientpattern</strong><br>沿着给定长度的向量设置调色板方块</td><td>中级(Intermediate)</td></tr><tr><td>ezedits.region.moss</td><td><strong>//ezmoss</strong><br>在平坦表面上放置一层方块</td><td>中级(Intermediate)</td></tr><tr><td>ezedits.region.noisegen</td><td><strong>//eznoisegen ...</strong><br>使用噪声在区域内放置方块</td><td>中级(Intermediate)</td></tr><tr><td>ezedits.region.solidslab</td><td><strong>//ezslabmerge</strong><br>将双层台阶转换为完整方块</td><td>基础(Basic)</td>

</tr><tr><td>ezedits.region.statecycle</td><td><strong>//ezstatecycle</strong><br>循环切换具有该状态的所有方块的给定方块状态</td><td>中级(Intermediate)</td></tr><tr><td>ezedits.region.vines</td><td><strong>//ezvines</strong><br>从给定蒙版悬挂像藤蔓一样的方块</td><td>中级(Intermediate)</td></tr><tr><td>ezedits.scatter</td><td><strong>//ezscatter</strong><br>在选区内方块表面散布形状或3D结构</td><td>高级(Advanced)</td></tr><tr><td>ezedits.selection.delete</td><td><strong>//ezselection delete</strong><br>删除已保存的选区</td><td>中级(Intermediate)</td></tr><tr><td>ezedits.selection.delpos</td><td><strong>//-2</strong><br>移除选区中的最后一个点</td><td>基础(Basic)</td></tr><tr><td>ezedits.selection.encapsulate</td><td><strong>//encapsulate</strong><br>将现有选区调整为包含匹配方块的最小长方体</td><td>基础(Basic)</td></tr><tr><td>ezedits.selection.encapsulatenear</td><td><strong>//encapsulatenear</strong><br>创建一个最小尺寸的长方体选区以包含附近的匹配方块</td><td>基础(Basic)</td></tr><tr><td>ezedits.selection.here</td><td><strong>//selhere</strong><br>将选区移动到玩家位置</td><td>基础(Basic)</td></tr><tr><td>ezedits.selection.invert</td><td><strong>//selinvert</strong><br>反转选区中点的顺序</td><td>基础(Basic)</td></tr><tr><td>ezedits.selection.list</td><td><strong>//ezselection list</strong><br>列出所有已保存的选区</td><td>中级(Intermediate)</td></tr><tr><td>ezedits.selection.load</td><td><strong>//selload</strong><br>加载已保存的选区</td><td>中级(Intermediate)</td></tr><tr><td>ezedits.selection.next</td><td><strong>//next</strong><br>将选区沿给定方向移动其自身大小的距离</td><td>基础(Basic)</td></tr><tr><td>ezedits.selection.save</td><td><strong>//ezselection save</strong><br>保存当前选区</td><td>中级(Intermediate)</td></tr><tr><td>ezedits.smooth.deflate</td><td><strong>//ezdeflate</strong><br>收缩选区中的方块</td><td>基础(Basic)</td></tr><tr><td>ezedits.smooth.ezsmooth</td><td><strong>//ezsmooth</strong><br>在3D空间中平滑选区</td><td>中级(Intermediate)</td></tr><tr><td>ezedits.smooth.inflate</td><td><strong>//ezinflate</strong><br>扩展选区中的方块</td>

<td>基础(Basic)</td></tr><tr><td>ezedits.smooth.smoothblocks</td><td><strong>//ezsmoothblocks</strong><br>使用楼梯、台阶和墙平滑区域</td><td>高级(Advanced)</td></tr><tr><td>ezedits.spline.basic</td><td><strong>//ezspline basic</strong><br>生成一个简单的圆柱形样条</td><td>中级(Intermediate)</td></tr><tr><td>ezedits.spline.2d</td><td><strong>//ezspline 2d ...</strong><br>通过沿路径扫掠2D形状生成成型样条</td><td>中级(Intermediate)</td></tr><tr><td>ezedits.spline.3d</td><td><strong>//ezspline 3d ...</strong><br>基于给定的样条形状生成样条</td><td>中级(Intermediate)</td></tr><tr><td>ezedits.spline.expression</td><td><strong>//ezspline expression</strong><br>通过给定表达式生成成型样条</td><td>中级(Intermediate)</td></tr><tr><td>ezedits.spline.noise</td><td><strong>//ezspline noise</strong><br>生成使用噪声变形的样条</td><td>中级(Intermediate)</td></tr><tr><td>ezedits.spline.rope</td><td><strong>//ezspline rope</strong><br>生成绳索形状的样条</td><td>中级(Intermediate)</td></tr><tr><td>ezedits.spline.structure</td><td><strong>//ezspline structure ...</strong><br>沿样条路径拉伸结构</td><td>中级(Intermediate)</td></tr><tr><td>ezedits.stainedglass.gradient</td><td><strong>//stainedglassgradient</strong><br>使用染色玻璃层生成颜色渐变</td><td>基础(Basic)</td></tr><tr><td>ezedits.stainedglass.image</td><td><strong>//stainedglassimage</strong><br>使用染色玻璃层生成图像</td><td>实验性(Experimental)</td></tr><tr><td>ezedits.surface.fuzzify</td><td><strong>//ezsurface fuzzify</strong><br>使用白噪声使区域内方块表面更加"模糊"</td><td>中级(Intermediate)</td></tr><tr><td>ezedits.surface.noisify</td><td><strong>//ezsurface noisify</strong><br>使用给定噪声变形区域内方块表面</td><td>中级(Intermediate)</td></tr><tr><td>ezedits.surface.rockify</td><td><strong>//ezsurface rockify</strong><br>使用噪声使区域内方块表面更加"岩石化"</td><td>中级(Intermediate)</td></tr><tr><td>ezedits.surface.voronoify</td><td><strong>//ezsurface voronoify</strong><br>使用沃罗诺伊噪声使区域内方块表面更加细胞化</td><td>中级(Intermediate)

</tr><tr><td>ezedits.survival.deinfest</td><td><strong>//ezsurvival deinfest</strong><br>将被虫蚀的方块替换为其未被虫蚀的对应方块</td><td>基础(Basic)</td></tr><tr><td>ezedits.survival.persist</td><td><strong>//ezsurvival persist</strong><br>让你设置选区内所有树叶的持久性,启用或禁用衰减</td><td>基础(Basic)</td></tr><tr><td>ezedits.survival.wax</td><td><strong>//ezsurvival wax</strong><br>将铜方块替换为其涂蜡或未涂蜡的对应方块</td><td>基础(Basic)</td></tr><tr><td>ezedits.symmetry.reflectional</td><td><strong>//ezsymmetry reflectional | reflectional3d</strong><br>启动反射对称模式</td><td>实验性(Experimental)</td></tr><tr><td>ezedits.symmetry.rotational</td><td><strong>//ezsymmetry rotational</strong><br>启动旋转对称模式</td><td>实验性(Experimental)</td></tr><tr><td>ezedits.symmetry.translational</td><td><strong>//ezsymmetry translational</strong><br>启动平移对称模式</td><td>实验性(Experimental)</td></tr><tr><td>ezedits.symmetry.update</td><td><strong>//ezsymmetry update</strong><br>更新方块以匹配相对于对称类型的变化</td><td>实验性(Experimental)</td></tr><tr><td>ezedits.texture.advanced</td><td><strong>//eztexture advanced</strong><br>混合多个纹理</td><td>中级(Intermediate)</td></tr><tr><td>ezedits.texture.ambient</td><td><strong>//eztexture ambient</strong><br>通过近似环境光为区域添加纹理</td><td>中级(Intermediate)</td></tr><tr><td>ezedits.texture.axisgradient</td><td><strong>//eztexture axisgradient</strong><br>使用轴对齐渐变为区域添加纹理</td><td>中级(Intermediate)</td></tr><tr><td>ezedits.texture.blend</td><td><strong>//eztexture blend</strong><br>混合相邻的调色板方块</td><td>中级(Intermediate)</td></tr><tr><td>ezedits.texture.blocklight</td><td><strong>//eztexture blocklight</strong><br>根据游戏内方块光照等级为区域添加纹理</td><td>中级(Intermediate)</td></tr><tr><td>ezedits.texture.cells</td><td><strong>//eztexture cells</strong><br>使用沃罗诺伊噪声绘制细胞纹理</td><td>中级(Intermediate)</td></tr><tr><td>ezedits.texture.curvature</td><td><strong>//eztexture curvature</strong><br>通过近似3D曲率为区域添加纹理</td><td>中级(Intermediate)</td></tr><tr><td>ezedits.texture.surfaceflow</td><td><strong>//eztexture flow</strong><br>沿表面使用流场为区域添加纹理</td>

<td>中级(Intermediate)</td></tr><tr><td>ezedits.texture.noise</td><td><strong>//eztexture noise</strong><br>使用给定的噪声对区域进行纹理处理</td><td>中级(Intermediate)</td></tr><tr><td>ezedits.texture.pointlight</td><td><strong>//eztexture pointlight</strong><br>根据表面朝向点光源的程度对区域进行纹理处理</td><td>中级(Intermediate)</td></tr><tr><td>ezedits.texture.shift</td><td><strong>//eztexture shift</strong><br>将调色板中的方块按给定数量进行偏移</td><td>中级(Intermediate)</td></tr><tr><td>ezedits.texture.sunlight</td><td><strong>//eztexture sunlight</strong><br>根据表面朝向全局光照方向的程度对区域进行纹理处理</td><td>中级(Intermediate)</td></tr><tr><td>ezedits.tools.superwand</td><td><strong>//superwand</strong><br>一个高级选区工具</td><td>基础(Basic)</td></tr></tbody></table>